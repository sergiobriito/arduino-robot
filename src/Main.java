import javax.swing.*;

public class Main {
    public static void main(String[] args) {
        SelectGridFrame interfaceFrame = new SelectGridFrame();
        interfaceFrame.setDefaultCloseOperation(JFrame.DISPOSE_ON_CLOSE);
        interfaceFrame.setVisible(true);
    }
}